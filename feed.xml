<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://stephan271c.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://stephan271c.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-12-08T01:31:37+00:00</updated><id>https://stephan271c.github.io/feed.xml</id><title type="html">blank</title><entry><title type="html">Test Time Regression</title><link href="https://stephan271c.github.io/blog/2025/Test-Time-Regression/" rel="alternate" type="text/html" title="Test Time Regression"/><published>2025-12-07T00:00:00+00:00</published><updated>2025-12-07T00:00:00+00:00</updated><id>https://stephan271c.github.io/blog/2025/Test-Time-Regression</id><content type="html" xml:base="https://stephan271c.github.io/blog/2025/Test-Time-Regression/"><![CDATA[<p>An important task in language modeling is associative recall: to associate a <strong>key</strong> with a <strong>value</strong> and retrieve that value when queried with the key. For example, after processing “The capital of France is Paris,” the model should associate the key “capital of France” with the value “Paris.” When it needs to recall this fact later, it can query its memory and retrieve the correct information.</p> <p>Our exposition follows <d-cite key="wang2025testtime"></d-cite>: we will analyze associative recall as a regression problem, re-cast it as an online learning problem, and then examine the memory capacity of memory modules.</p> <h2 id="problem-setup">Problem Setup</h2> <p>In sequence modeling with transformers, we have the following:</p> <ul> <li>the input is a sequence of tokens \(x_1,...,x_T\) and the output is \(y_1,..,y_T\).</li> <li>we learn embedding vectors for the tokens \(x_1,...,x_T\).</li> <li>we learn projection matrices \(\mathbf{W}_Q,\mathbf{W}_K, \mathbf{W}_V\) to map embeddings to query, key, and value vectors. The query vector \(\mathbf{q}_{t}\)​ isn’t necessarily one of the key vectors; this allows the model to combine information from multiple values. Nevertheless, the idea is that we want to be able to learn associations between tokens and return the relevant token.</li> </ul> <p>In a transformer’s self attention layer, we keep track of the KV cache, which at time \(t\) is the key value pairs \(S_t=\{(\mathbf{k}_1,\mathbf{v}_1),...,(\mathbf{k}_t,\mathbf{v}_t)\}\). We then get \(\mathbf{y}_t=\text{SelfAttn}(S_t,\mathbf{q}_t)=\sum_{i=1}^{t} \frac{s(\mathbf{k}_i, \mathbf{q}_t)}{\sum_{j=1}^{t} s(\mathbf{k}_j, \mathbf{q}_t)} \mathbf{v}_i\) where \(s(\mathbf{k}, \mathbf{q}) = \exp\left(\frac{\mathbf{k}^\top \mathbf{q}}{\sqrt{d_k}}\right)\).</p> <p>This can be framed as an associative learning task: Given \((\mathbf{k}_1,\mathbf{v}_1),...,(\mathbf{k}_t,\mathbf{v}_t)\in \mathbb{R}^{d_k} \times \mathbb{R}^{d_v}\) we want to calculate some notion of state \(S_t\) which we use to form a mapping \(m_t=F(S_t).\) This mapping \(m_t:\mathbb{R}^{d_k} \rightarrow \mathbb{R}^{d_v}\) should remember the key value pairs \(m_t(\mathbf{k}_i)\approx \mathbf{v}_i\) and it returns \(\mathbf{\hat{y}}_t=m_t(\mathbf{q}_t)\). Then we update the state \(S_{t+1}=f(S_{t},(\mathbf{k}_{t+1},\mathbf{v}_{t+1}))\).</p> <p>One issue with standard softmax attention is keeping track of the KV cache \(S_t\), which grows proportionally with sequence length. And performing softmax for time step \(t\) takes \(O(t)\) time. What if we can choose \(S_t\) to take constant memory, and choose \(m_t\) so that calculating \(m_t(\mathbf{q}_t)\) takes \(O(1)\) time?</p> <p>Our problem consists of 3 parts:</p> <ul> <li><strong>Choose the function class \(\mathcal{F}\) where our memory module \(m_t\) lives:</strong> here, the state \(S_t\) can represent weights \(\theta_{t}\) of \(m_t\) if \(m_t=f_{\theta_t}\) is from a parametric function class. Or \(S_t\) can be key value pairs if \(m_t\) is from a nonparametric function class.</li> <li><strong>Choose the loss function:</strong> In order to select \(m_{t}\) from \(\mathcal{F}\), we need to minimize some objective. Consider the key and value vectors observed so far: \(\mathbf{K}_t=[\mathbf{k}_1,...,\mathbf{k}_t]\) and \(\mathbf{V}_t=[\mathbf{v}_1,...,\mathbf{v}_t]\). We want \(m_t=\text{argmin}_{m\in \mathcal{F}}L(m(\mathbf{K}_t), \mathbf{V}_t)\) for some loss function \(L\). We assume the loss decomposes as \(L(m(\mathbf{K}_t), \mathbf{V}_t)=\sum_{i=1}^t c_i\ell(m(\mathbf{k}_i), \mathbf{v}_i)+\lambda R(m)\). The function \(\ell\) is usually \(L_2\) loss: \(\ell(\mathbf{a},\mathbf{b})=\frac{1}{2}\lVert \mathbf{a}-\mathbf{b} \rVert ^2_2\). Since \(m_t\) is being chosen/learned at the forward pass, this is called test time regression.</li> <li><strong>Choose the optimizer:</strong> instead of solving \(\text{argmin}\) exactly, we can use an approximate algorithm.</li> </ul> <p>If we pick a parametric function class, then \(S_{t}\) will take constant memory and will not grow over time. However, computing the loss still requires us to keep track of the sequence of key and value vectors. Later we will consider an online learning approach that allows us to solve for \(m_{t}\) approximately and have constant memory complexity.</p> <h2 id="matrix-memory-modules">Matrix Memory Modules</h2> <p>Consider the class of linear functions \(\mathcal{F}=\{m \vert m(\mathbf{k})=\mathbf{Mk}, \ \mathbf{M}\in \mathbb{R}^{d_v \times d_k}\}\).</p> <p>For the loss function, let’s consider a weighted ridge regression: a weighted sum of squares \(\frac{1}{2} \sum_{i=1}^{t} c_{i}\left \Vert \mathbf{v}_{i} - \mathbf{Mk}_{i}\right \Vert_{2}^{2}\) with a regularization term \(R(\mathbf{M})=\lambda \left \Vert \mathbf{M}\right \Vert_F.\) Let \(\mathbf{K}_t\in \mathbb{R}^{t \times d_k }\) and \(\mathbf{V}_t \in \mathbb{R}^{t \times d_v }\) be the key and value matrices, and denote \(\mathbf{C}_{t}=\text{diag}(c_{i})\in \mathbb{R}^{t \times t}\). We have the general formula:</p> \[\begin{align*} m_t &amp;= \text{argmin}_{\mathbf{M}\in \mathbb{R}^{d_v \times d_k}} \frac{1}{2} \sum_{i=1}^{t} c_{i}\left \Vert \mathbf{v}_{i} - \mathbf{Mk}_{i}\right \Vert_{2}^{2}+\lambda \left \Vert \mathbf{M}\right \Vert_F^2 \\ &amp;= \text{argmin}_{\mathbf{M}\in \mathbb{R}^{d_v \times d_k}}\frac{1}{2} \left \Vert (\mathbf{V}_t^\top - \mathbf{MK}_t^\top)\mathbf{C}^{1/2}\right \Vert_{F}^{2} +\lambda \left \Vert \mathbf{M}\right \Vert_F^2 \\ &amp;= \begin{cases} {\mathbf{V}_{t}}^{\top}\mathbf{C}_{t} \mathbf{K}_{t} (\mathbf{K}_{t}^{\top}\mathbf{C}_{t} \mathbf{K}_{t}+2\lambda \mathbf{I})^{-1}, &amp; t \geq d_{k} \\ {\mathbf{V}_{t}}^{\top} (\mathbf{K}_{t} {\mathbf{K}_{t}}^{\top}+2\lambda \mathbf{C}_{t})^{-1} \mathbf{K}_{t}, &amp; t &lt; d_{k} \end{cases} \end{align*}\] <p>This is the objective in mesa optimizers <d-cite key="voswald2024uncovering"></d-cite>.</p> <p>Instead of directly solving for \(m_{t}\) for each time step, we can actually compute it as a recurrence using the Sherman-Morrison formula:</p> <ul> <li>Let \(\mathbf{P}_{t}={\mathbf{V}_{t}}^{\top}\mathbf{C}_{t} \mathbf{K}_{t}\)</li> <li>Let \(\mathbf{R}_{t}=(\mathbf{K}_{t}^{\top}\mathbf{C}_{t} \mathbf{K}_{t}+2\lambda \mathbf{I})^{-1}\)</li> <li>Update \(\mathbf{P}_{t+1}=\mathbf{P}_{t}+{c_{t+1}}\mathbf{v}_{t+1}\mathbf{k}_{t+1}^\top\) and \(\mathbf{R}_{t+1} = \mathbf{R}_{t} - \frac{c_{t+1} (\mathbf{R}_{t} \mathbf{k}_{t+1}) (\mathbf{k}_{t+1}^\top \mathbf{R}_{t})}{1 + c_{t+1} \mathbf{k}_{t+1}^\top \mathbf{R}_{t} \mathbf{k}_{t+1}}\)</li> </ul> <h3 id="connection-to-linear-attention">Connection to Linear Attention:</h3> <p>From the loss function, drop the regularization term, assume \(\mathbf{C}_{t}=\mathbf{I}\), and \(\mathbf{K}^\top_t \mathbf{K}_t \approx \mathbf{I}\). Then we get \(\mathbf{M}_t=\mathbf{V}^\top_t \mathbf{K}_t=\sum_{i=1}^t \mathbf{v}_i \mathbf{k}_i^\top = \mathbf{M}_{t-1}+\mathbf{v}_t\mathbf{k}_t^\top\). This is the recurrence of the state matrix in linear attention. In this way, linear attention can be thought of as a suboptimal solution to the least squares problem where we assume the key covariance matrix is the identity matrix.</p> <h3 id="connection-to-gated-linear-attention">Connection to Gated Linear Attention</h3> <p>Now consider time decaying weights, \(c_{i}=\gamma_i^{(t)}=\prod_{j=i+1}^t \gamma_i\) where \(\gamma_i\in[0,1]\). Let’s also drop the regularization term:</p> \[m_t=\underset{\mathbf{M}\in \mathbb{R}^{d_v \times d_k} }{\text{argmin}}\frac{1}{2} \sum_{i=1}^{t} \gamma_i^{(t)}\left \Vert \mathbf{v}_{i} - \mathbf{Mk}_{i}\right \Vert_{2}^{2}\] <p>We can apply the previous closed form solution after rescaling the matrices: \(\mathbf{V}_t \mapsto\mathbf{\Gamma}_t^{\frac{1}{2}}\mathbf{V}_t\) and \(\mathbf{K}_t \mapsto\mathbf{\Gamma}_t^{\frac{1}{2}}\mathbf{K}_t\) where \(\mathbf{\Gamma}_t\in\mathbb{R}^{t\times t}\) is the diagonal matrix with \((\mathbf{\Gamma}_t)_{ii}=\gamma_i^{(t)}\). The solution becomes</p> \[m_t= \begin{cases} {\mathbf{V}_{t}}^{\top}\mathbf{\Gamma}_t \mathbf{K}_{t} (\mathbf{K}_{t}^{\top}\mathbf{\Gamma}_t \mathbf{K}_{t})^{-1}, &amp; t \geq d_{k} \\ {\mathbf{V}_{t}}^{\top} (\mathbf{K}_{t} {\mathbf{K}_{t}}^{\top})^{-1} \mathbf{K}_{t}, &amp; t &lt; d_{k} \end{cases}\] <p>If we further assume \(\mathbf{K}_{t}^{\top}\mathbf{\Gamma}_t \mathbf{K}_{t}\approx \mathbf{I}\), we get </p> \[\mathbf{M}_t\approx\sum_{i=1}^{t} \gamma^{(t)}_i \mathbf{v}_i\mathbf{k}_i^\top=\gamma^t\sum_{i=1}^{t-1} \gamma^{(t-1)}_i \mathbf{v}_i\mathbf{k}_i^\top+\mathbf{v}_t\mathbf{k}_t^\top=\gamma^t\mathbf{M}_{t-1}+\mathbf{v}_t\mathbf{k}_t^\top\] <p>this is the recurrence for gated linear attention, and it shows how it is a suboptimal solution to weighted least squares.</p> <h2 id="softmax-attention-as-nonparametric-regression">Softmax Attention as Nonparametric Regression</h2> <p>Here we generalize the notion of inner product to a higher dimensional kernel space. Let \(k:\mathbb{R}^{d_{k}\times d_{k}}\rightarrow \mathbb{R}\) be a continuous symmetric positive-definite kernel. Mercer’s theorem allows us to express the kernel as \(k(\mathbf{x},\mathbf{y})=\phi(\mathbf{x})^\top \phi(\mathbf{y})\) for a possibly infinite dimensional feature map \(\phi: \mathbb{R}^{d_{k}}\to \mathbb{R}^{d_{\phi}}.\)</p> <p>Suppose we want to solve \(\mathbf{m}_{t}=\text{argmin}_{\mathbf{m}\in \mathcal{F}} \sum_{i=1}^t \lVert \mathbf{v}_{i}-\mathbf{M}\phi(\mathbf{k}_{i}) \rVert^2_{2}\) where \(\mathcal{F}=\{ m \vert {m}(\mathbf{k})=\mathbf{M}\phi(\mathbf{k}), \mathbf{M}\in \mathbb{R}^{d_{v}\times d_{\phi}} \}\). The solution is given by </p> \[{m}_{t}(\mathbf{q})=\mathbf{V}_{t}^\top \mathbf{k}(\mathbf{K}_{t},\mathbf{K}_{t})^{-1} \mathbf{k}(\mathbf{K}_{t},\mathbf{q})\] <p>where \({k}(\mathbf{K}_{t},\mathbf{K}_{t})_{ij}=k(\mathbf{k}_{i},\mathbf{k}_{j})\). Here we are assuming that \(t\leq d_{\phi}\). If we choose a polynomial kernel, then \({m}_{t}\) can be expressed as a \(d_{v} \times d_{\phi}\) matrix \(\mathbf{M}\). On the other hand, if we choose an infinite dimensional kernel, then the state becomes the \(t \times t\) gram matrix \({k}(\mathbf{K}_{t},\mathbf{K}_{t})\), which increases with sequence length \(t\).  Suppose we use the exponential kernel \(k(\mathbf{k}_{i},\mathbf{k}_{j})=\exp (\mathbf{k}_{i}^\top \mathbf{k}_{j}/\sqrt{ d_{k} })\) and assume \({k}(\mathbf{K}_t,\mathbf{K}_{t})\approx \mathbf{I}\). Then \({m}_{t}(\mathbf{q})\approx \mathbf{V}^\top_{t}{k}(\mathbf{K}_{t},\mathbf{q})  =\sum_{i=1}^t \mathbf{v}_{i}k(\mathbf{k}_{i}, \mathbf{q})=\sum_{i=1}^t \mathbf{v}_{i} \exp\left( \frac{\mathbf{k}^\top_{i}\mathbf{q}}{\sqrt{ d_{k} }} \right)\). This is unnormalized softmax, and can be considered a suboptimal solution to the nonparametric regression problem.</p> <h2 id="online-learning-framework">Online Learning Framework</h2> <p>In the previous sections, solved the least squares problem exactly for each time step. Here we compute approximate solutions by casting the problem into an online learning framework. This provides a lot of flexibility in choosing the loss and the optimizer.</p> <p>Online learning is a sequential decision making problem where a learner makes predictions at each round, with the goal of minimizing the cumulative loss <d-cite key="Shalev-Shwartz_2011"></d-cite>. We have the following problem setup:</p> <ul> <li>at each time \(t\), pick an action \(a_t\in \mathcal{A}\) from a convex set \(\mathcal{A}\)</li> <li>observe the convex loss function \(\ell_t:\mathcal{A}\rightarrow \mathbb{R}\)</li> <li>receive loss \(\ell_t(a_t)\)</li> </ul> <p>The goal is to minimize the regret by time horizon \(T\), which is \(\text{regret}_T=\sum_{t=1}^T\ell_t(a_t)-\min_{a\in \mathcal{A}}\sum_{t=1}^T\ell_t(a)\)</p> <p>We can now interpret test time learning as an online learning problem:</p> <ul> <li>at time \(t\), pick a function \(m_{t-1}\in \mathcal{F}\) (to be elaborated below)</li> <li>observe \((\mathbf{k}_t,\mathbf{v}_t)\), which produces a loss function \(\ell_t(m)=\left \Vert m(\mathbf{k}_t)-\mathbf{v}_t \right \Vert_2^2\)</li> <li>receive loss \(\ell_t(m_{t-1})\)</li> <li>in the next round, choose \(m_{t}\). For example, we can take a gradient descent step \(m_t=m_{t-1}-\eta_t \nabla \ell_t(m_{t-1})\). In other scenarios, we choose \(m_{t}=\text{argmin}_{m\in\mathcal{F}}\ell_{t}(m)\).</li> <li>we want to minimize regret by time \(T\), which is the end of the sequence. In other words: \(\text{regret}_{T}=\sum_{t=1}^T \left \Vert m_{t-1}(\mathbf{k}_t)-\mathbf{v}_t \right \Vert_2^2-\min_{m\in \mathcal{F}}\sum_{t=1}^T \left \Vert m(\mathbf{k}_t)-\mathbf{v}_t \right \Vert_2^2\).</li> </ul> <p>The benefit of the online learning approach is that we have natural way to expand on this:</p> <ul> <li>changing the function class to be neural networks <d-cite key="sun2025learning"></d-cite>.</li> <li>modifying the loss function to be least squares of a set of points <d-cite key="behrouz2025atlas"></d-cite>.</li> <li>choose another optimizer.</li> </ul> <p>Here are some examples:</p> <div class="table-responsive" style="overflow-x: auto; -webkit-overflow-scrolling: touch;"> <table class="table table-sm"> <thead> <tr> <th>Method</th> <th>State</th> <th>online loss $\ell_t(m)$</th> <th>optimizer</th> <th>state update rule</th> </tr> </thead> <tbody> <tr> <td>Linear Attention <d-cite key="katharopoulos2020transformers"></d-cite></td> <td>$\mathbf{M}_{t}\in \mathbb{R}^{d_{v}\times d_{k}}$</td> <td>$-\langle \mathbf{Mk}_{t},\mathbf{v}_{t} \rangle$</td> <td>GD ($\eta_{t}=1$)</td> <td>$\mathbf{M}_{t}=\mathbf{M}_{t-1}+\mathbf{v}_{t}\mathbf{k}_{t}^\top$</td> </tr> <tr> <td>RetNet <d-cite key="sun2023retentive"></d-cite></td> <td>$\mathbf{M}_{t}\in \mathbb{R}^{d_{v}\times d_{k}}$</td> <td>$-\langle \mathbf{Mk}_{t},\mathbf{v}_{t} \rangle+\frac{1-\alpha}{2}\lVert \mathbf{M} \rVert^2_{F}$</td> <td>GD ($\eta_{t}=1$)</td> <td>$\mathbf{M}_{t}=\alpha\mathbf{M}_{t-1}+\mathbf{v}_{t}\mathbf{k}_{t}^\top$</td> </tr> <tr> <td>DeltaNet <d-cite key="yang2025parallelizing"></d-cite></td> <td>$\mathbf{M}_{t}\in \mathbb{R}^{d_{v}\times d_{k}}$</td> <td>$\frac{1}{2} \left \Vert \mathbf{M} \mathbf{k}_t-\mathbf{v}_t \right \Vert^2_2$</td> <td>GD</td> <td>$\mathbf{M}_t=\mathbf{M}_{t-1}(1-\eta_t\mathbf{k}_{t}\mathbf{k}_{t}^\top)+\eta_t\mathbf{v}_t\mathbf{k}_t^\top$</td> </tr> <tr> <td>Regularized DeltaNet</td> <td>$\mathbf{M}_{t}\in \mathbb{R}^{d_{v}\times d_{k}}$</td> <td>$\frac{1}{2} \left \Vert \mathbf{M} \mathbf{k}_t-\mathbf{v}_t \right \Vert^2_2+\frac{\lambda_{t}}{2}\lVert \mathbf{M} \rVert^2_{F}$</td> <td>GD</td> <td>$\mathbf{M}_t=\mathbf{M}_{t-1}((1-\eta_{t}\lambda_{t})-\eta_t\mathbf{k}_{t}\mathbf{k}_{t}^\top)+\eta_t\mathbf{v}_t\mathbf{k}_t^\top$</td> </tr> <tr> <td>Gated DeltaNet <d-cite key="yang2025gated"></d-cite></td> <td>$\mathbf{M}_{t}\in \mathbb{R}^{d_{v}\times d_{k}}$</td> <td>$\lVert M-\alpha_{t}M_{t-1} \rVert^2_{F}-2\langle Mk_{t},\beta_{t}(v_{t}-\alpha_{t}M_{t-1}k_{t}) \rangle$</td> <td>exact</td> <td>$\mathbf{M}_t=\mathbf{M}_{t-1}(\alpha_{t}(1-\beta_t\mathbf{k}_{t}\mathbf{k}_{t}^\top))+\beta_t\mathbf{v}_t\mathbf{k}_t^\top$</td> </tr> <tr> <td>TTT MLP <d-cite key="sun2025learning"></d-cite></td> <td>parameters $\theta_{t}$ of neural network $f$</td> <td>$\frac{1}{2} \left \Vert f_{\theta}(\mathbf{k}_t)-\mathbf{v}_t \right \Vert^2_2$</td> <td>GD (fixed $\eta$)</td> <td>$\theta_{t}=\theta_{t-1}-\eta \nabla L_{t}(\theta_{t-1})$</td> </tr> <tr> <td>Titans <d-cite key="behrouz2024titans"></d-cite></td> <td>parameters $\theta_{t}$</td> <td>$\frac{1}{2} \left \Vert f_{\theta}(\mathbf{k}_t)-\mathbf{v}_t \right \Vert^2_2$</td> <td>GD w/ momentum\* (w/ weight decay)</td> <td>$\theta_{t}=\alpha_{t}\theta_{t-1}+\beta_{t}$<br/>$\beta_{t}=\mu_{t}\beta_{t-1}-\eta_{t} \nabla L_{t}(\theta_{t-1})$</td> </tr> <tr> <td>Atlas <d-cite key="behrouz2025atlas"></d-cite></td> <td>parameters $\theta_{t}$</td> <td>$\sum_{i=t-c+1}^t\frac{\gamma_{i}}{2} \left \Vert f_{\theta}(\mathbf{k}_i)-\mathbf{v}_i \right \Vert^2_2$</td> <td>Muon (w/ weight decay)</td> <td>$\theta_{t}=\alpha_{t}\theta_{t-1}-\eta_{t}\text{NS}(\beta_{t})$ (Newton -Schultz) $\beta_{t}=\mu_{t}\beta_{t-1}+ \nabla L_{t}(\theta_{t-1})$</td> </tr> </tbody> </table> </div> <p>Note that for Titans and Atlas we have a decoupled weight decay, in the style of AdamW. Also, the momentum formulation in Titans is slightly different in that the learning rate \(\eta_{t}\) is on the gradient and not the momentum term \(\beta_{t}\).</p> <h2 id="memory-capacity-of-memory-modules">Memory Capacity of Memory Modules</h2> <p>A natural question to ask is how many key value pairs can a memory module remember. Suppose the key and value dimension are equal to \(d\). If we have \(d\) pairs of vectors \((\mathbf{k}_{i},\mathbf{v}_{i})\) such that \(\mathbf{k}_{i}\) are linearly independent, we can always find an \(d\times d\) matrix \(\mathbf{M}\) such that \(\mathbf{v}_{i}=\mathbf{Mk}_{i}\). However, in language modeling, we don’t have to return the value vector exactly. Instead, we want to choose the token associated with the value vector. Suppose we have \(N\) key-value pairs and we use “hard-max” attention: we want a vector \(\hat{\mathbf{v}}=\mathbf{Mk}_{i}\) that maximizes a score function \(s\) so that \(i=\underset{j\in[N]}{\text{argmax}}\ s(\hat{\mathbf{v}},\mathbf{v}_{j}).\) In this section we will consider the inner product as the scoring function: \(s(\hat{\mathbf{v}},\mathbf{v}_{j})=\hat{\mathbf{v}}^\top \mathbf{v}_{j}\).</p> <p>It turns out that the number of key value pairs a matrix memory module can retain is proportional to the number of parameters in the matrix (up to logarithmic factors).</p> <p>Here we introduce some results from <d-cite key="nichani2024understanding"></d-cite>. Suppose \(d=d_{v}=d_{k}\) and consider a set of input tokens \([N]\) and a set of output tokens \([M]\). We want to learn an association, given by \(f^*:[N]\to [M]\). Assume that \(f^*(i)=i\). For each \(x\in[N]\), assign an embedding vector \(\mathbf{k}_{x}\in \mathbb{R}^d\) and for each \(y\in[M]\) assign an unembedding vector \(\mathbf{v}_{y}\in \mathbb{R}^d\), where both vectors lie on the unit sphere: \(\mathbf{k}_{x}, \mathbf{v}_{y}\in \mathbb{S}^{d-1}\). Let \(m:\mathbb{R}^d \to \mathbb{R}^d\) be our memory module, which will learn key value associations, \(m(\mathbf{k}_{i}) \approx\mathbf{v}_{i}\). Our prediction is given by argmax decoding \(\hat{f}(x):=\text{argmax}_{y\in [M]}\mathbf{v}_{y}^\top m(\mathbf{k}_{x})\). If \(m\) is a linear map \(m(\mathbf{k}_{x})=\mathbf{M}\mathbf{k}_{x}\), we have the following result:</p> <h4 id="theorem-1-">Theorem 1 <d-cite key="nichani2024understanding"></d-cite>:</h4> <p>Assume that \(f^*\) is injective. If \(d^2 \gtrsim N \cdot \text{poly log} N\), then with high probability over the draw of the embeddings, there exists a \(\mathbf{W}\) such that \(\arg\max_{y \in [M]} \mathbf{u}_y^\top \mathbf{We}_x = f^*(x) \quad \text{for all } x \in [N].\) This capacity is obtained by the construction \(W = \sum_{x \in [N]} \mathbf{u}_{f^*(x)} \mathbf{e}_x^\top\).</p> <p>Now consider a 2 layer neural network with hidden dimension \(c\), given by \(m(\mathbf{k})=\mathbf{V}\sigma(\mathbf{Wk})\) for matrices \(\mathbf{V},\mathbf{W}\in \mathbb{R}^{c\times d}\). At first, we might think that the greater expressivity of neural networks can translate to a greater number of key-value pairs remembered. However, we get a similar result as the linear case:</p> <h4 id="theorem-2-">Theorem 2 <d-cite key="nichani2024understanding"></d-cite>:</h4> <p>If \(cd \gtrsim N \cdot \text{poly log} N\), then with high probability over the draw of the embeddings, there exists \(V, W\) such that \(\arg\max_{y \in [M]} \mathbf{u}_y^\top \mathbf{V}^\top \sigma(\mathbf{We}_x) = f^*(x) \quad \text{for all } x \in [N].\) Note that both linear and nonlinear memory modules have a capacity that is proportional to the number of parameters (ignoring log factors). This suggests that in terms of capacity alone, nonlinear memory modules don’t have an advantage. However, with nonlinear modules, \(f^*\) does not have to be injective, which means several key vectors can be associated to a single value vector.</p> <h2 id="online-learning-for-matrix-memories">Online Learning for Matrix Memories</h2> <p>Now let’s look at numerical experiments to see how different matrix memory modules fare. You can find the code <a href="https://github.com/stephan271c/in-context-recall">here.</a> We compare 3 modules:</p> <ul> <li>Linear Attention</li> <li>DeltaNet (denoted TTT)</li> <li>Mesa Layer For the TTT module, instead of using only the online loss \(L_{t}(\mathbf{M})= \left \Vert \mathbf{M} \mathbf{k}_t-\mathbf{v}_t \right \Vert^2_2\), we introduce a context size parameter \(\text{ctx}\) so that the loss becomes \(L_{t}(\mathbf{M})=\sum_{i=0}^{\text{ctx}-1}\left \Vert \mathbf{M} \mathbf{k}_t-\mathbf{v}_t \right \Vert^2_2\).</li> </ul> <p>For the data, we sample \(20\)-dimensional key and value vectors of unit length. By default, each vector is drawn i.i.d, but later we change the pairwise correlation of the key vectors, so that \(\text{corr}(k_{i},k_{i-1})\neq 0\). We sample 500 sequences of length 100 and plot the average accuracy and recall count.</p> <h3 id="evaluation-metrics">Evaluation metrics</h3> <p>We consider two metrics: accuracy by offset, and retrieval counts by timestep.</p> <h4 id="accuracy-by-offset">Accuracy by offset</h4> <p>After updating our memory module \(\mathbf{M}_{t}=\text{update}(\mathbf{M}_{t-1},(\mathbf{k}_{t},\mathbf{v}_{t}))\), we test whether it can retrieve the value vector \(\mathbf{v}_{i}\) when given the key vector \(\mathbf{k}_{i}\). In other words, we compute \(\frac{1}{\text{# of seq}}\sum_{s=1}^{\text{# of seq}}\mathbf{1}_{i=r_{i}}\), where \(r_{i}=\text{argmax}_{j\in[0,t]}\mathbf{v}^\top_{j}(\mathbf{M}_{t}\mathbf{k}_{i})\). Note that we compare inner product scores of only the value vectors seen so far, \(\mathbf{v}_{0},\dots \mathbf{v}_{t}\). This is analogous to causal language modeling with transformers, where we compute the attention scores against previously seen tokens. We then organize these retrieval accuracies by offset: we look at how well \(\mathbf{M}_{t}\) retrieves \(\mathbf{v}_{t-k}\) for fixed \(k\), as \(t\) varies from \(1\) to the sequence length. Note that the accuracy calculation involves a decreasing number of subsequences as the offset increases; at offset \(k\), we can only include retrieval counts when \(t\geq k\).</p> <h4 id="retrieval-counts-by-timestep">Retrieval counts by timestep</h4> <p>Here we sum up all the correct retrievals of \(\mathbf{M}_{t}\) for each timestep \(t\). If the memory module is able to retrieve all previously seen key value pairs, the graph should look like the line \(y=x\).</p> <h3 id="empirical-results">Empirical results</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-acc-480.webp 480w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-acc-800.webp 800w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-acc-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-acc.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>In this graph, we plot recall accuracy by offset, with a context size of 1. Note that linear attention and Mesa Layer attention are independent of the context size.</p> <p>Due to the exponential forgetting parameter \(\gamma = 0.9\), the Mesa Layer perfectly recalls the most recent key value pairs. On the other hand, linear attention has a nonzero retrieval accuracy across all offsets. This echos the observation that linear attention lacks a forgetting mechanism, so it cannot properly attend to new tokens once its capacity is saturated. The TTT layer does seem to have a forgetting mechanism, with a higher accuracy for more recent KV pairs. Additionally, a larger learning rate corresponds to faster learning and faster decay of recall accuracy.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc-480.webp 480w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc-800.webp 800w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Here we examine the TTT layer with context size 5. This effectively means that the matrix memory can be updated 5 times for a single KV pair through gradient descent. Note that the accuracy at the first few offsets is low, and it reaches perfect accuracy faster when using a larger learning rate. It seems that a single gradient step isn’t enough for the TTT layer.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc-480.webp 480w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc-800.webp 800w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-acc.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Here we introduce pairwise correlation between the key vectors, so that \(\text{corr}(\mathbf{k}_{i},\mathbf{k}_{i-1})=0.7\). Both linear attention and the TTT layer suffer in performance. This makes sense, as neither memory module incorporates the covariance matrix \(\mathbf{K}_{t}^\top\mathbf{K}_{t}\) into its update equation. On the other hand, the mesa layer maintains good performance and retains the same shape curve.</p> <div class="row mt-3"> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-ret-480.webp 480w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-ret-800.webp 800w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-ret-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx1-ret.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-ret-480.webp 480w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-ret-800.webp 800w,/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-ret-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-12-07-Test-Time-Regression/corr0-ctx5-ret.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm-4 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-12-07-Test-Time-Regression/corr0.7-ctx5-ret-480.webp 480w,/assets/img/2025-12-07-Test-Time-Regression/corr0.7-ctx5-ret-800.webp 800w,/assets/img/2025-12-07-Test-Time-Regression/corr0.7-ctx5-ret-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-12-07-Test-Time-Regression/corr0.7-ctx5-ret.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Here we see the retrieval counts by timestep. For all memory modules, the retrieval count saturates at much lower numbers than what is theoretically possible, which should be roughly proportional to the number of parameters in the matrix. However, in the 0 input correlation case, the counts saturate at above 20 counts, which is more than the maximum rank of our \(20\times 20\) matrix. Note that for the TTT layers and linear attention, the retrieval count starts to decrease as the timestep increases. It’s likely that even the TTT layers aren’t fully forgetting past context, negatively affecting retrieval capacity later in the sequence. On the other hand, the retrieval count remains stable for the Mesa Layer.</p> <h2 id="conclusion-and-limitations">Conclusion and Limitations</h2> <p>We gave an overview of associative recall and how it can be framed as an online learning problem. We also looked at a toy example of doing associative recall on key value pairs of vectors on the unit sphere. A few things we didn’t cover are more advanced optimizers, as well as multi-layer TTT. They didn’t do as well in preliminary experiments, likely because the vectors are sampled randomly for each sequence, so there’s less structure to exploit. In language modeling, we learn the embedding matrices as well as the projection matrices, so it’s possible for more sophisticated memory modules to perform better. We also didn’t consider computational efficiency- often times a paper will introduce a memory module and then implement it using an approximation.</p>]]></content><author><name></name></author><category term="linear-RNNs"/><category term="associative-recall"/><category term="online-learning"/><category term="transformers"/><summary type="html"><![CDATA[Associative Recall and connections to Online Learning]]></summary></entry><entry><title type="html">State Tracking with Sequence Models</title><link href="https://stephan271c.github.io/blog/2025/State-Tracking/" rel="alternate" type="text/html" title="State Tracking with Sequence Models"/><published>2025-07-04T00:00:00+00:00</published><updated>2025-07-04T00:00:00+00:00</updated><id>https://stephan271c.github.io/blog/2025/State-Tracking</id><content type="html" xml:base="https://stephan271c.github.io/blog/2025/State-Tracking/"><![CDATA[<p>Large Language Models can perform impressive tasks such as storytelling, instruction following, and code generation. These capabilities seem to suggest an ability to maintain a complex internal “state,” keeping track of context and dependencies over long sequences. And yet, their performance often falters on precise compositional tasks, such as logical puzzles and arithmetic, which involve keeping track of intermediate results <d-cite key="dziri2023faithfatelimitstransformers"></d-cite>.</p> <p>Most LLMs are based on the transformer architecture, as it performs well on language modeling. However, under a standard conjecture in computational complexity theory, transformers may not be well suited to the state tracking task.</p> <p>We’ll define state tracking, introduce relevant concepts from circuit complexity, analyze how Transformers and RNNs perform, discuss limitations, and explore potential solutions.</p> <h2 id="what-is-state-tracking">What is State Tracking?</h2> <p>Formally, we can consider state tracking as simulating the transition function of a semiautomaton, following <d-cite key="liu2023transformerslearnshortcutsautomata"></d-cite>:</p> <p>Let \(\mathcal{A} = (Q,\Sigma, \delta)\) be a <em>semiautomaton</em>, which consists of a finite set of states \(Q\), an input alphabet \(\Sigma\), and a transition function \(\delta : Q \times \Sigma \rightarrow Q.\) We can think of \(\sigma \in \Sigma\) as actions or transformations that update the state from \(q\) to \(q^\prime=\delta(q, \sigma)\). Given a starting state \(q_0\) and an input sequence \(\sigma_{1:T} = (\sigma_1,\sigma_2,...,\sigma_T)\) we get a sequence \((q_1,...,q_T)\) by the action \(q_t=\delta (q_{t-1},\sigma_t)\) for \(t=1,...,T.\) This defines a mapping \(\mathcal{A}_{T,q_0}: \Sigma^T \rightarrow Q^T\) by \(\mathcal{A}_{T,q_0} (\sigma_1,\sigma_2,...,\sigma_T) = (q_1,...,q_T).\)</p> <p>We say that a function \(f:\Sigma^T \rightarrow Q^T\) <em>simulates</em> \(\mathcal{A}\) if for all input sequences \(\sigma_{1:T}\), \(f(\sigma_{1:T}) = \mathcal{A}_{T,q_0}(\sigma_{1:T})\). Usually, \(f\) is chosen to be a sequence model like a Transformer or RNN.</p> <p>To simplify our discussion, we will define the <em>state tracking problem</em> as the task of finding the final state, given the initial state \(q_0\) and an input sequence \((\sigma_1,\sigma_2,...,\sigma_T)\). We focus on this final state computation to understand the limits of models tackling this problem.</p> <h2 id="introduction-to-circuit-complexity">Introduction to Circuit Complexity</h2> <p>To analyze these limits, we can use the framework of <em>circuit complexity</em>, which measures the resources required to compute a function using a family of boolean circuits, denoted \(\{C_n\}^\infty _{n=0}.\) This framework usually considers functions with output in \(\{ 0, 1\}\), so computations need to be reformulated into a decision problem. In the context of our state tracking problem, for input size \(T\), we want a function \(f\) such that \(f(\sigma_1,\sigma_2,...,\sigma_T) =1\) if \(\pi_T\left(\mathcal{A}_{T,q_0}(\sigma_1,\sigma_2,...,\sigma_T) \right)=q_T\) and \(0\) otherwise.</p> <p><strong>Definition:</strong> A <em>boolean circuit</em> \(C_n\) is a function \(\{0,1\}^n \rightarrow \{0,1\}\), represented by a directed acyclic graph with 3 types of nodes:</p> <ul> <li><strong>Input nodes:</strong> \(n\) nodes with an in-degree of \(0\), corresponding to the \(n\) input bits.</li> <li><strong>Gate nodes:</strong> Nodes with positive in-degree and out-degree, representing intermediate computations. Each is labeled with a boolean gate (e.g., AND, OR, NOT, MAJORITY).</li> <li><strong>Output node:</strong> A single node with an out-degree of 0, representing the circuit’s output.</li> </ul> <p>We analyze a family of boolean circuits \(\{C_n\}_{n=0}^\infty\) to describe how computational resources scale with the input size \(n\). The number of nodes in \(C_n\) is its <strong>size</strong>, \(s(n)\), and the length of the longest path from an input to the output is its <strong>depth</strong>, \(d(n)\).</p> <p><strong>Definition.</strong> Let \(\Sigma\) be a non-empty set, called an alphabet. Consider the free monoid construction, \(\Sigma^*=\cup _{i\geq 0} \Sigma^i\) where \(\Sigma^i\) denotes i-fold concatenation and \(\Sigma^0 = \{ \epsilon \}\) is the set containing the empty string. \(\Sigma^*\) is the set of strings of finite length generated by the alphabet \(\Sigma,\) including the empty string \(\epsilon\). The operator \(^*\) is called the Kleene star operator. A <em>formal language</em> is a subset \(L \subseteq \Sigma^*\). We are interested in binary languages, \(L \subseteq \{0,1\}^*\) and in particular its <em>characteristic function</em> \(f_L: \{0,1\}^* \rightarrow \{0,1\},\) where \(f_L(x) =1\) if and only if \(x \in L.\)</p> <p><strong>Example:</strong> Consider the language \(L_{PARITY}=(11)^*=\{\epsilon, 11,1111,....\}\) which contains all strings of \(1\)’s of even length. The characteristic function is \(f(1^k)=1\) if \(k\) is even, and \(f(1^k)=0\) if \(k\) is odd.</p> <p><strong>Definition:</strong> A family of boolean circuits \(\{C_n\}_{n=0}^\infty\) <em>recognizes a language</em> \(L\) if for any \(n\geq 0\) and string \(x\in\{0,1\}^n,\) \(x\in L\) if and only if \(C_n(x)=1.\) We can also formulate this using the characteristic function: for any \(x\in \{0,1\}^n,\) \(f_L(x)=C_n(x)\).</p> <p><strong>Definition:</strong> A function \(f: \{0,1\}^* \rightarrow \{0,1\}\) is in \(TC^0\) if there exists a polynomial \(p\), a constant \(d\), and a family of boolean circuits \(\{C_n\}_{n=0}^\infty\) such that:</p> <ul> <li>each circuit is made of AND, OR, NOT and MAJORITY gates of unbounded fan in,</li> <li>each \(C_n\) is of size \(p(n)\),</li> <li>has constant depth \(d\)</li> <li>for an input of size \(n\), \(x \in \{0,1\}^n\), we have \(f(x)=C_n(x)\).</li> </ul> <p><strong>Definition:</strong> A function \(f: \{0,1\}^* \rightarrow \{0,1\}\) is in \(NC^1\) if there exists a polynomial \(p\), a constant \(d\), and a family of boolean circuits \(\{C_n\}_{n=0}^\infty\) such that:</p> <ul> <li>each circuit is made of AND, OR, NOT gates of fan in 2,</li> <li>each \(C_n\) is of size \(p(n)\),</li> <li>\(C_n\) has logarithmic depth \(O(\log(n))\),</li> <li>for an input of size \(n\), \(x \in \{0,1\}^n\), we have \(f(x)=C_n(x)\).</li> </ul> <p><strong>Definition:</strong> a language \(L\) is in \(TC^0\) (or \(NC^1\)) if its characteristic function \(f_L\) is in the corresponding class.</p> <p>The definitions above can be extended to functions with multiple output bits: let \(in(n), out(n)\) be non-negative, non-decreasing, polynomially bounded functions, and consider boolean circuits \(C_n:\{0,1\}^{in(n)} \rightarrow \{0,1\}^{out(n)}\). For example, \(n\)-bit addition is a function \(f:\{0,1\}^{2n}\rightarrow \{0,1\}^{n+1}\) <d-cite key="doi:10.1137/0215070"></d-cite>.</p> <p>Although state tracking and related problems of interest will be represented with binary valued functions, some intermediate computations will involve multiple bit outputs. The main point is that some functions can be computed with constant depth circuits (in \(TC^0\)), while others need logarithmic depth (in \(NC^1\)).</p> <p><strong>Examples:</strong></p> <ul> <li>\(n\) bit addition, multiplication and division is in \(TC^0\)</li> <li>\(L_{PARITY}\) is in \(TC^0\)</li> <li>A computation with fixed sized input is vacuously in \(TC^0\). \(k \times k\) matrix multiplication for fixed \(k\) is in \(TC^0\).</li> </ul> <p>A standard fact in circuit complexity theory is that \(TC^0 \subseteq NC^1\). It’s an open problem whether the containment is strict, although it is believed that \(TC^0 \neq NC^1\).</p> <h2 id="state-tracking-and-circuit-complexity">State Tracking and Circuit Complexity</h2> <p>Note that the state tracking problem can be solved by a circuit in \(NC^1\) by formulating it as a matrix multiplication problem. For each state \(q\in Q\), we can encode it into a \(|Q|\)-dimensional one-hot vector. Each input symbol \(\sigma \in \Sigma\) gives us a map \(\delta(-, \sigma):Q \rightarrow Q\) which we can think of as a matrix \(M_\sigma \in \{0,1\}^{|Q| \times |Q| }\). We have \([M_\sigma]_{i,j}=1_{\{ \delta(e_j , \sigma) = e_i \}}\) and \(M_\sigma\) acts on state vectors by left multiplication. This matrix multiplication can be performed by a constant depth circuit, and we can apply a divide and conquer approach to perform all the multiplications in a \(O(\log T)\) depth circuit. Therefore, state tracking is in \(NC^1\).</p> <p>Using log precision, Transformers are in \(TC^0\) <d-cite key="merrill2023parallelismtradeofflimitationslogprecision"></d-cite>. If we believe \(TC^0 \neq NC^1\), then a constant depth transformer would not be able to solve the state tracking problem for arbitrary sequence length. This could explain why transformers fail to generalize to longer sequences and harder problems. If we allow variable depth based on input size, we can construct a transformer with \(O(\log T)\) layers to solve the state tracking problem of length \(T\) <d-cite key="liu2023transformerslearnshortcutsautomata"></d-cite>. While a transformer performing next token prediction may not be as expressive as an RNN, allowing it to output intermediate tokens can increase expressivity. A transformer model performing chain of thought for \(O(n)\) intermediate steps <d-cite key="merrill2024expressivepowertransformerschain"></d-cite> can simulate \(NC^1\) circuits (see also <d-cite key="li2024chainthoughtempowerstransformers"></d-cite>).</p> <p>Recurrent Neural Networks (RNNs) can solve state tracking in a single layer. An RNN maps a sequence \((x_1,...,x_T)\) to \((y_1,...y_T)\) and is given by the recurrence</p> \[h_{t+1}=g(h_t, x_t), \ y_t=dec(h_t,x_t)\] <p>where \(g\) and \(dec\) are learnable, nonlinear functions. Let \(x_t=\sigma_t\) and \(h_t =q_{t-1}\) be the 1-hot vector representing the state. \(g(-,x_t)\) can simulate the state transition function \(\delta (- ,\sigma_t)\) (<d-cite key="merrill2025illusionstatestatespacemodels"></d-cite>, theorem 5.1). In particular, even if we constrain \(g(-,x_t)\) to be a linear transformation, it can represent the state transition matrix \(M_{\sigma_t}\), and so linear RNNs with general state matrix can also solve the state tracking problem.</p> <p>Although state tracking and other relevant problems lie in \(NC^1\), we will focus on a specific synthetic task, to which any other task in \(NC^1\) can be reduced.</p> <h3 id="the-word-problem-a-proxy-for-nc1">The Word Problem: A Proxy for \(NC^1\)</h3> <p><em>Word problem for a finite group</em> \(G:\) given a sequence \((x_1,x_2,...x_n) \in G^n\) and \(y \in G\), determine if \(x_1 x_2...x_n = y\). It is equivalently formulated as: given a sequence \((g_1,...,g_n) \in G^n\), determine if \(g_1g_2,...,g_n = e\).</p> <ul> <li>For solvable groups, like \(A_4\) and \(\mathbb{Z}_{60}\), this is in \(TC^0\) (via Khoan Rhoades theorem).</li> <li>For nonsolvable groups (such as \(A_5\) and \(S_5\)), it is \(NC^1\) complete.</li> </ul> <p>Barrington’s theorem <d-cite key="BARRINGTON1989150"></d-cite> states that any circuit in \(NC^1\), with input size \(n\), can be reduced to a circuit calculating the word problem on \(A_5\) of polynomial length in \(n\). (Thms 1 and 5). This means solving the \(A_5\)​ word problem is, in a sense, as hard as any other problem in \(NC^1\), including state tracking. We will therefore use the \(A_5\)​ word problem as a benchmark for evaluating model capabilities.</p> <h3 id="no-of-layers-vs-sequence-length">No. of Layers vs Sequence Length</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-04-State-Tracking/graphs_3tasks-480.webp 480w,/assets/img/2025-07-04-State-Tracking/graphs_3tasks-800.webp 800w,/assets/img/2025-07-04-State-Tracking/graphs_3tasks-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-07-04-State-Tracking/graphs_3tasks.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Now let’s show some empirical results showing how transformers do on this problem. For \(G \in \{\mathbb{Z}_{60}, A_4 \times \mathbb{Z}_5, A_5 \}\), we train llama based transformers to acheive &gt;90% validation accuracy, following the experiment from <d-cite key="merrill2025illusionstatestatespacemodels"></d-cite>. We directly encode group elements into tokens instead of tokenizing its string based representation.</p> <p>Here, the standard task is: fix an maximum sequence length \(n\). For \(k\leq n\), given an input sequence \((x_1,...,x_k)\in G^k\), we want our model to calculate \(f_\theta(x_1,...,x_k)=y_k\) where \(y_k=x_1 \cdot ...\cdot x_k\).</p> <p>The chain of thought task is: \(f_\theta (x_1,...,x_n, c, y_1,...,y_k)=y_{k+1}\) where \(c\) is a placeholder token. For \(k=0\) we have \(f_\theta (x_1,...,x_n, c)=y_{1}\). In particular, in <d-cite key="li2024chainthoughtempowerstransformers"></d-cite> they construct a 2 layer transformer that can solve the word problem on \(S_5\).</p> <p>A similar idea to CoT is pursued in <d-cite key="liu2023transformerslearnshortcutsautomata"></d-cite>, called “scratchpad training” where the output tokens are interleaved with the input tokens \((x_1,y_1,x_2,y_2,...,x_n,y_n)\) and the model is trained autoregressively. Intuitively, these tasks are easier because the model can look at its intermediate results to perform the next calculation, avoiding the need for logarithmic depth.</p> <p>In the figure, we see that for \(A_5\), the transformer model needs more layers to handle longer sequences, while the LSTM model can predict with just one layer. The transformer model in Chain of Thought mode also only needs 1 layer to solve this task. Interestingly, for the transformer model, the word problem for $A_4\times \mathbb{Z}_5$ is almost as hard as the $A_5$ word problem, requiring more layers as the sequence length increases.</p> <h2 id="linear-rnns">Linear RNNs</h2> <p>Linear Recurrent Neural Networks are motivated by the goal of developing fast, efficient sequence models capable of performance comparable to Transformers, particularly in language modeling. Unlike a traditional RNN, the hidden state is updated through an affine transformation. The condition that it’s affine allows us to use parallel associative scan for the forward pass. We’ll follow notation from <d-cite key="grazzi2025unlockingstatetrackinglinearrnns"></d-cite>:</p> <p><strong>Definition:</strong> A linear RNN layer is a function \(f_\theta : \mathbb{R}^l \times \mathbb{C}^{n\times d} \rightarrow \mathbb{R}^l \times \mathbb{C}^{n\times d}\) that maps an input, hidden state pair \((x_t, H_{t-1})\) to output, next hidden state pair \((\hat y_t, H_{t})\) in the following way:</p> \[\begin{gathered} H_t = A(x_t)H_{t-1}+B(x_t),\ \hat y_t = dec(H_t,x_t)\ \text{for } t\in \{ 1,...,T\} \\ H_0 \in\mathbb{C}^{n\times d},\ A:\mathbb{R}^l \rightarrow \mathbb{C}^{n\times n},\ B:\mathbb{R}^l \rightarrow \mathbb{C}^{n\times d},\ dec: \mathbb{C}^{n\times d} \times\mathbb{R}^l \rightarrow \mathbb{R}^p \end{gathered}\] <p>where \(A,B\) and \(dec\) are learnable, and generally nonlinear functions of the network parameters \(\theta\). We can stack multiple layers by feeding the outputs of one layer as the inputs to the next layer. For the rest of this post we will focus on real valued \(A\) and \(B\).</p> <p>The structure of the state transition matrix function \(A\) determines the expressivity of the linear RNN. If we allow \(A\) to be a general real valued matrix, we can simulate a semiautomaton by setting \(A\) to be the state transition matrix. However, we constrain \(A\) in various ways in order to train them efficiently. For stable training, we bound the eigenvalues \(\lvert \lambda_i \rvert \leq 1\). This addresses the gradient exploding problem usually seen with traditional RNNs.</p> <p>Initially, popular linear RNN architectures constrained the matrix \(A\) to being diagonal and with nonnegative eigenvalues. Some examples are Mamba and Gated Linear Attention. It turns out these choices limit the expressivity of the model:</p> <p><strong>Theorem 1:</strong> (from <d-cite key="grazzi2025unlockingstatetrackinglinearrnns"></d-cite>) For a finite precision LRNN with finitely many layers, if the eigenvalues of each \(A(x)\) is nonnegative for all x, then it cannot can solve parity for arbitrary input lengths. In other words, it cannot recognize the language \((11)^*\)</p> <p><em>Proof idea:</em> Let’s consider a single layer LRNN, as multiple layers can be handled by induction. For simplicity, we will assume all arithmetic operations will be handled in \(\mathbb{R},\) with infinite precision, and then the cast the result into finite precision \(\mathbb{D}\), which is a finite subset of \(\mathbb{R}.\) The closed form expression for the hidden state \(H_k\) is</p> \[H_k = \left( \prod^k_{i=1} A(x_i)\right)H_0+ \sum^k_{i=1}\left( \prod^k_{j=i+1}A(x_j) \right)B(x_i)\] <p>where we set \(\prod^k_{j=k+1}A(x_j) = I\). On an input sequence \(x_1,...,x_k=(1)^k\), we get</p> \[H_k = A(1)^k H_0 + \sum^{k}_{i=1}A(1)^{i-1}B(1)\] <p>Now write \(A(1)=PJP^{-1}\) in its Jordan canonical form and consider its largest eigenvalue \(\lambda_1\) in \(J\). After analyzing the different cases \(\lambda_1 \in [0,1), \lambda_1 = 1\), and \(\lambda_1 &gt;1\), there exists \(\tau\) such that for all \(k \geq \tau\) and a fixed matrix \(M,\) the resulting matrix product becomes constant after taking finite precision: \(cast(A(1)^kM)=\hat M\). Thus for \(k\) large enough, \(cast(H_k) = cast(H_{k+1})\) and the LRNN cannot distinguish between \((1)^k\) and\((1)^{k+1}\).</p> <p><strong>Theorem 2:</strong> (from <d-cite key="grazzi2025unlockingstatetrackinglinearrnns"></d-cite>): For a finite precision LRNN with finitely many layers, if the eigenvalues of each \(A(x)\) is real for all \(x\), then it cannot can solve modular counting \(\mod m\), where \(m\) is not a power of \(2\), for arbitrary input lengths. In other words, it cannot recognize the language \((1^m)^*\). Note that for a real valued triangular matrix, all of its eigenvalues are real, so a LRNN with triangular state matrix cannot solve modular counting. This motivates the need for general, non-triangular state matrices.</p> <p><em>Proof idea:</em> It’s very similar to the proof of theorem 1, although with negative eigenvalues, a single layer LRNN will have its hidden state alternate between \(2\) values, for sequence length \(k\) large enough. For \(L\) layers, the function \(k \mapsto \hat H_k\) will be periodic with period \(2^L\), so if \(m \neq 2^n\) for some \(n\), then the LRNN cannot recognize modular counting.</p> <p>Architectures that allow for negative eigenvalues and non-triangular matrices include modifications of DeltaNet and DeltaProduct, as well as RWKV v7. In DeltaProduct, the state-transition matrix \(A(x_{i})\) is defined as the product of \(n_h\) generalized Householder matrices: \(A(x_{i}) = \prod_{j=1}^{n_{h}}(I - \beta_{i,j}k_{i,j}k_{i,j}^{\top}).\) To allow for negative eigenvalues, they replace $\beta_{i,j}$ with $2\beta_{i,j}$, where $\beta_{i,j}\in [0,1]$ is an input dependent gating factor. This construction enables a rank \(n_h\)​ update to the hidden state at each step. Since a \(k\times k\) permutation matrix can be formed by a product of \(k-1\) GH matrices, a single layer DeltaProduct model with \(n_h=k-1\) and \(n=k\) can solve the word problem of the permutation group on \(k\) elements (see theorem 3 in <d-cite key="grazzi2025unlockingstatetrackinglinearrnns"></d-cite>): simply choose generalized Householder matrices such that \(A(x_i)\in \mathbb{R}^{k \times k}\) is the permutation matrix representing \(x_i\) and \(H_i \in \mathbb{R}^{k\times 1}\) is the resulting permutation on \(k\) elements after \(i\) permutation operations.</p> <div class="row mt-3"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-04-State-Tracking/A5-480.webp 480w,/assets/img/2025-07-04-State-Tracking/A5-800.webp 800w,/assets/img/2025-07-04-State-Tracking/A5-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-07-04-State-Tracking/A5.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-04-State-Tracking/S5-480.webp 480w,/assets/img/2025-07-04-State-Tracking/S5-800.webp 800w,/assets/img/2025-07-04-State-Tracking/S5-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-07-04-State-Tracking/S5.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figures from <d-cite key="siems2025deltaproductimprovingstatetrackinglinear"></d-cite>. Comparing accuracy vs sequence length for modified DeltaProduct models with $n_h$ GH matrices and coefficients $2\beta_{i,j}$. </div> <p>In <d-cite key="siems2025deltaproductimprovingstatetrackinglinear"></d-cite>, they empirically show that the word problem on a permutation group on \(5\) elements can be solved with a single layer DeltaProduct with \(4\) GH matrices. Interestingly, the model with \(2\) \(GH\) matrices is able to learn permutations from \(A_5\) by exploiting the structure of the group.</p> <h2 id="conclusion">Conclusion</h2> <p>While a transformer model has a hard time solving compositional tasks like word problems, chain of thought allows it to reuse previous calculations for the next decoding step. On the other hand, linear RNNs, when the state matrix is expressive enough, can solve the word problem without chain of thought. It would be interesting to see if LLMs based on linear RNNs are better at solving compositional tasks in natural language with a shorter chain of thought than current transformer based reasoning LLMs.</p>]]></content><author><name></name></author><category term="state-tracking"/><category term="circuit-complexity"/><category term="compositional-generalization"/><category term="transformers"/><summary type="html"><![CDATA[Analyzing the theoretical limits of sequence models for state tracking and compositional reasoning]]></summary></entry></feed>